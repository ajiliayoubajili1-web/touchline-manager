"""Game API layer.

This is the single surface the UI talks to: validated commands (actions) and
read-only view models (queries). There is no UI or storage logic here beyond
using the save system.
"""